import * as planck from 'planck';
import { Renderer } from './renderer';

export function drawJoints(
  ctx: CanvasRenderingContext2D,
  world: planck.World,
  renderer: Renderer
): void {
  // Joint rendering will be implemented after body rendering is complete
}

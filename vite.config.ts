import { defineConfig } from 'vite';

export default defineConfig({
  // Use the repo name as the base so assets load correctly on GitHub Pages.
  // When running `vite dev` locally the base defaults to '/' automatically.
  base: '/PhysicsKitchen/',
});
